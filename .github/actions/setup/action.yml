name: Setup
description: Setup pnpm and Node

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v6
      with:
        version: 8

    - uses: actions/setup-node@v6
      with:
        node-version: 20
        cache: pnpm
