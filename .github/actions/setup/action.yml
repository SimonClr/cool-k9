name: Setup
description: Setup pnpm and Node

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v6
      with:
        version: 8

    - uses: actions/setup-node@v6
      with:
        # Aligned with the hosting platform runtime and the local dev environment.
        node-version: 22
        cache: pnpm
